{
  "name": "taskme",
  "version": "1.0.0",
  "description": "TaskMe - Team Task Management Web Application",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
